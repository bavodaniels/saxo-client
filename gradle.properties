# Dependency Verification Configuration
# Enables checksum verification for all resolved dependencies to prevent tampering
# The verification metadata file is auto-discovered at ./gradle/verification-metadata.xml
org.gradle.dependency.verification=lenient
org.gradle.dependency.verification.checksums=sha256
